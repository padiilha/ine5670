require("../model/db-connection");
const Aluguel = require("../model/aluguel");

const create = (hora_inicio, hora_fim, st_patinete, telefone) => {};
const read = (id) => {};
const update = (id, hora_inicio, hora_fim, st_patinete, telefone) => {};